import java.util.Scanner;

public class LongerLine {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        double x1plane1 = Double.parseDouble(scanner.nextLine());
        double y1plane1 = Double.parseDouble(scanner.nextLine());
        double x2plane1 = Double.parseDouble(scanner.nextLine());
        double y2plane1 = Double.parseDouble(scanner.nextLine());

        double x1plane2 = Double.parseDouble(scanner.nextLine());
        double y1plane2 = Double.parseDouble(scanner.nextLine());
        double x2plane2 = Double.parseDouble(scanner.nextLine());
        double y2plane2 = Double.parseDouble(scanner.nextLine());

    }

}
